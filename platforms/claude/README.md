# Claude adapter — placeholder

Not implemented yet.

This directory reserves the future Claude host adapter. It should reuse the same repo-scoped `.chatgpt-worker.toml`, discovery output, path mappings, validation rules, and worker-loop semantics rather than duplicating shared logic.
