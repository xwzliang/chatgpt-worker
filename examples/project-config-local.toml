execution = "local"

branch_prefix = "chatgpt-worker/"
max_iterations = 5

[validation]
commands = [
  "npm run typecheck",
  "npm test",
  "npm run build",
]
[communication]
runtime_dir = ".chatgpt-worker"
retain_on_merge = false

