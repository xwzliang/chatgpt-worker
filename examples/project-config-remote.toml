execution = "remote"

branch_prefix = "chatgpt-worker/"
max_iterations = 5

[remote]
host = "ai-server"
repo_roots = [
  "/mnt/omv/git",
  "/home/broliang/git",
]

[[remote.path_mappings]]
remote = "/mnt/omv"
local = "/Volumes/omv"

[validation]
commands = [
  "pytest -q",
  "ruff check .",
]
